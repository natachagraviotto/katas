// Crea una función que devuelva el número negativo del parámetro 4

inverse(4) // Debe mostrar -4
inverse(2) // Debe mostrar -2


function inverse(numero) {
    return -numero;
  }
  
console.log(inverse(4)); // Imprime -4

// En este ejemplo, hemos definido la función inverse que toma un parámetro numero, lo multiplica por -1 y devuelve el resultado. Luego hemos llamado a esta función con un argumento de 4 utilizando console.log, lo que imprimirá el resultado de la función -4 en la consola.

// Por lo tanto, la salida del código será -4, que es el número negativo de 4.

function inverse(numero) {
    return -numero;
  }
  
console.log(inverse(2)); // Imprime -4