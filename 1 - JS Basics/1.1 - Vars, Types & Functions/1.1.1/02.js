// Encuentra el error en este código

let a = 4;
a = 5;

console.log(a);

// El error en este código es que se está intentando asignar un nuevo valor a una constante (a) después de haberla declarado.
// En JavaScript, una constante se declara y se inicializa con un valor que no puede ser cambiado posteriormente. Si intentamos asignar un nuevo valor a una constante, se producirá un error.

//Por lo tanto, para corregir el error, debemos usar una variable en lugar de una constante, o asignar un valor a a solo en su declaración. Aquí hay un ejemplo corregido que utiliza una variable:

// En este código, hemos declarado a como una variable en lugar de una constante, lo que nos permite cambiar su valor en cualquier momento. Al ejecutar este código, verá que el valor de a es 5, que es el valor que se le asignó en la segunda línea.
